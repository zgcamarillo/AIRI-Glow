version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build"
    build:
      commands:
        - echo "Nothing to build - static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
      - 'assets/**/*'